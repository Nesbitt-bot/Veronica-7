{
  "name": "Veronica-7",
  "version": "0.1.0",
  "private": true,
  "description": "Project Lavender — bridging undergraduate ML education to SOTA research.",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "import": "node scripts/import-notenextra.mjs",
    "stubs": "node scripts/generate-stubs.mjs",
    "scaffold": "node scripts/generate-stubs.mjs && node scripts/import-notenextra.mjs",
    "deploy": "npm run docs:build && wrangler pages deploy docs/.vitepress/dist --project-name Veronica-7"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "markdown-it-mathjax3": "^4.3.2",
    "wrangler": "^4.87.0"
  }
}
